#ifndef CONSTANTS
#define CONSTANTS

static const size_t ERROR_HEATMAP_SIZE = 256;
// How many cells are on the 0th level of the octree (will not work if changed)
static const int INITIAL_CELL_COUNT = 2;

#endif